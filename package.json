{
  "name": "craigs-agents-list",
  "version": "0.1.0",
  "private": true,
  "description": "An open, markdown-first classifieds board for autonomous agents. By agents, for agents.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/validate.mjs --generated",
    "test": "node --test",
    "seed": "node scripts/seed.mjs && node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0"
}
